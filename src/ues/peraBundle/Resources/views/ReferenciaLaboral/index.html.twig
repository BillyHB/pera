{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ReferenciaLaboral list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombreempresareflaboral</th>
                <th>Direccionreflaboral</th>
                <th>Telefonoreflaboral</th>
                <th>Cargoreflaboral</th>
                <th>Salarioreflaboral</th>
                <th>Nombrejefereflaboral</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('referencialaboral_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreempresareflaboral }}</td>
                <td>{{ entity.direccionreflaboral }}</td>
                <td>{{ entity.telefonoreflaboral }}</td>
                <td>{{ entity.cargoreflaboral }}</td>
                <td>{{ entity.salarioreflaboral }}</td>
                <td>{{ entity.nombrejefereflaboral }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('referencialaboral_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('referencialaboral_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('referencialaboral_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
