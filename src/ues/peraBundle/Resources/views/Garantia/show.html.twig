{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Garantia</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Tipogarantia</th>
                <td>{{ entity.tipogarantia }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('garantia') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('garantia_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
