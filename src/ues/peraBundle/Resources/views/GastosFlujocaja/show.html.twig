{% extends '::base.html.twig' %}

{% block body -%}
    <h1>GastosFlujocaja</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Compramercaderiagfc</th>
                <td>{{ entity.compramercaderiagfc }}</td>
            </tr>
            <tr>
                <th>Gastosnegociogfc</th>
                <td>{{ entity.gastosnegociogfc }}</td>
            </tr>
            <tr>
                <th>Gastosfamiliaresgfc</th>
                <td>{{ entity.gastosfamiliaresgfc }}</td>
            </tr>
            <tr>
                <th>Totalgfc</th>
                <td>{{ entity.totalgfc }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('gastosflujocaja') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('gastosflujocaja_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
