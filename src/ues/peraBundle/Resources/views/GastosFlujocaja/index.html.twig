{% extends '::base.html.twig' %}

{% block body -%}
    <h1>GastosFlujocaja list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Compramercaderiagfc</th>
                <th>Gastosnegociogfc</th>
                <th>Gastosfamiliaresgfc</th>
                <th>Totalgfc</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('gastosflujocaja_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.compramercaderiagfc }}</td>
                <td>{{ entity.gastosnegociogfc }}</td>
                <td>{{ entity.gastosfamiliaresgfc }}</td>
                <td>{{ entity.totalgfc }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('gastosflujocaja_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('gastosflujocaja_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('gastosflujocaja_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
