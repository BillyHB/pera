{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ActivoBg</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Disponibleabg</th>
                <td>{{ entity.disponibleabg }}</td>
            </tr>
            <tr>
                <th>Cuentasxcobrarabg</th>
                <td>{{ entity.cuentasxcobrarabg }}</td>
            </tr>
            <tr>
                <th>Activofijoabg</th>
                <td>{{ entity.activofijoabg }}</td>
            </tr>
            <tr>
                <th>Totalabg</th>
                <td>{{ entity.totalabg }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('activobg') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('activobg_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
